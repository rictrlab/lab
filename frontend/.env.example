# Local dev (this is also the fallback in lib/api.ts)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Production — set this as a Vercel Environment Variable pointing at your
# Hugging Face Space URL, e.g.:
# NEXT_PUBLIC_API_BASE=https://<hf-username>-rictrlab-api.hf.space
