import torch

def solve(x: torch.Tensor) -> torch.Tensor:
    """Swish activation: x * sigmoid(x).

    Args:
        x: Tensor of any shape

    Returns:
        Tensor of same shape with Swish applied.
    """
    pass
