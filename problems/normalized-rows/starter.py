import torch

def solve(x: torch.Tensor) -> torch.Tensor:
    """L2-normalize each row.

    Args:
        x: Tensor of shape (M, N) with non-zero rows.

    Returns:
        Tensor of same shape where each row has unit L2 norm.
    """
    pass
