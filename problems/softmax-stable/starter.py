import torch

def solve(x: torch.Tensor) -> torch.Tensor:
    """Stable softmax over last dimension.

    Args:
        x: Tensor of any shape

    Returns:
        Tensor of same shape with softmax applied over last dim.
    """
    pass
