import torch

def solve(a: torch.Tensor, b: torch.Tensor) -> torch.Tensor:
    """Add two tensors with broadcasting.

    Args:
        a: Tensor of shape (3, 1)
        b: Tensor of shape (1, 4)

    Returns:
        Tensor of shape (3, 4) with broadcasting addition.
    """
    pass
